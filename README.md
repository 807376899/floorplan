# floorplan

实验室分布图生成器：一个静态 HTML/SVG 小工具，用来把学校实验室表格数据转换成 JSON，并生成模拟的 2D 教室分布图。

## 迭代 1：测试数据字段

测试数据在 `sample-labs.csv`，表头使用中文字段。页面也兼容对应的英文字段名。核心字段如下：

- `教学楼`
- `楼层`
- `实验室名称`
- `南北侧`：填 `南` 或 `北`
- `前门牌`
- `后门牌`：单门房间可与前门牌相同
- `东到西门牌分布`：支持 `东到西递增` 或 `东到西递减`
- `长`、`宽`：按比例绘制房间
- `所属学院`：用于自动着色
- `走廊段`、`走廊方向`、`沿走廊位置`：用于表达主走廊、东翼、西翼等 L/T 型布局
- `分段X`、`分段Y`：可选，用米为单位标记走廊段在楼层中的相对位置；不填时自动上下排列

## 迭代 2：CSV/Excel 到 JSON

页面支持上传 `.csv`，也支持在联网时通过 SheetJS CDN 解析 `.xlsx` / `.xls` 的第一个工作表。导入后会标准化字段并在右侧显示 JSON 预览。页面底部提供可编辑表格，修改单元格后点击“应用修改”即可刷新分布图、缩略图和 JSON。

## 迭代 3：JSON 到 HTML/SVG 分布图

生成规则：

- 北侧房间绘制在走廊上方，南侧房间绘制在走廊下方。
- 东西向走廊支持 `北`、`南`、`西端`、`东端`；南北向走廊支持 `西`、`东`、`北端`、`南端`。
- SVG 左侧标为西，右侧标为东。
- 房间色块按照 `长`、`宽` 等比绘制，不再设置统一最小尺寸。
- 如果 `east_to_west_order` 是 `东到西递增`，图中从西到东按门牌号递减排列。
- 如果 `east_to_west_order` 是 `东到西递减`，图中从西到东按门牌号递增排列。
- 不同学院自动分配不同颜色，并生成图例。
- 同一幢楼的所有楼层会以缩略图从上到下排列，点击缩略图切换楼层。

## 数据建模建议

当前版本为了导入方便，仍支持所有信息放在一张房间表中。进入真实数据维护阶段后，建议拆成两类数据：

- 房间表：记录教学楼、楼层、门牌、长宽、学院、房间位置、沿走廊位置等。
- 楼层布局表：记录教学楼、楼层、走廊段、走廊方向、分段X、分段Y、走廊宽度、楼层形状类型等。

这样同一条走廊段不需要在每个房间行里重复填写，也更适合维护 L 型、T 型、回字型等复杂楼层。后续可以继续兼容单表导入，再在页面内转换成“房间数据 + 楼层布局数据”。

## 运行

在本目录启动任意静态服务器，然后打开 `index.html`。例如：

```powershell
python -m http.server 5173
```

然后访问 `http://localhost:5173`。
